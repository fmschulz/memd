{
  "_comment": "memd SessionStart hook for Codex CLI. Copy this into your project's .codex/hooks.json (merging with any existing hooks). It refreshes memory.md and kicks a background consolidation when enough new chunks have accumulated. The command is a no-op when the repo has no .memd scope file, so it is safe to leave wired everywhere.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "memd session-start --project-dir . 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
