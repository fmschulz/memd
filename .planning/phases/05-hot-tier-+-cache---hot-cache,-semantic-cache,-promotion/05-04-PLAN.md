---
phase: 05-hot-tier-+-cache
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - crates/memd/src/store/hybrid.rs
  - crates/memd/src/store/persistent.rs
  - crates/memd/src/metrics.rs
autonomous: true

must_haves:
  truths:
    - "HybridSearcher uses TieredSearcher for search operations"
    - "Tiered metrics (cache hits, hot tier hits, tier latencies) are tracked"
    - "PersistentStore exposes tiered search mode"
    - "Version numbers propagate for cache invalidation"
  artifacts:
    - path: "crates/memd/src/store/hybrid.rs"
      provides: "HybridSearcher with tiered integration"
      contains: "TieredSearcher"
    - path: "crates/memd/src/metrics.rs"
      provides: "Tiered metrics tracking"
      contains: "TieredMetrics"
  key_links:
    - from: "crates/memd/src/store/hybrid.rs"
      to: "crates/memd/src/tiered/tiered_searcher.rs"
      via: "tiered search on query"
      pattern: "tiered_searcher.search"
---

<objective>
Integrate TieredSearcher into HybridSearcher and PersistentStore, add tiered metrics collection.

Purpose: This plan wires the tiered architecture into the existing retrieval pipeline, replacing direct warm-tier search with cache/hot/warm fallback. Metrics are extended to track tier-specific performance.

Output: HybridSearcher using TieredSearcher, tiered metrics in MetricsCollector.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hot-tier-+-cache---hot-cache,-semantic-cache,-promotion/05-CONTEXT.md
@crates/memd/src/store/hybrid.rs
@crates/memd/src/store/persistent.rs
@crates/memd/src/metrics.rs
@crates/memd/src/tiered/tiered_searcher.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tiered metrics to MetricsCollector</name>
  <files>
    crates/memd/src/metrics.rs
  </files>
  <action>
Extend crates/memd/src/metrics.rs with tiered metrics:

Struct TieredMetrics:
- cache_lookups: u64
- cache_hits: u64
- cache_misses: u64
- cache_hit_rate: f32 (calculated)
- hot_tier_searches: u64
- hot_tier_hits: u64 (queries satisfied by hot tier alone)
- warm_tier_searches: u64
- promotions: u64
- demotions: u64
- avg_cache_lookup_ms: f64
- avg_hot_tier_ms: f64
- avg_warm_tier_ms: f64

Struct TieredQueryMetrics:
- source_tier: String ("cache" | "hot" | "warm" | "hybrid")
- cache_lookup_ms: u64
- hot_tier_ms: u64
- warm_tier_ms: u64
- cache_hit: bool
- hot_tier_hit: bool

Add to MetricsCollector:
- tiered_lookups: AtomicU64
- tiered_cache_hits: AtomicU64
- tiered_hot_hits: AtomicU64
- tiered_promotions: AtomicU64
- tiered_demotions: AtomicU64
- tiered_latencies: RwLock(Vec(TieredQueryMetrics))

Add methods:
- record_tiered_query(&self, metrics: TieredQueryMetrics)
- record_promotion(&self)
- record_demotion(&self)
- get_tiered_stats(&self) -> TieredMetrics

Update MetricsSnapshot:
- Add tiered: TieredMetrics field

Update snapshot() to include tiered stats.
  </action>
  <verify>cargo check passes</verify>
  <done>TieredMetrics struct and recording methods added to MetricsCollector</done>
</task>

<task type="auto">
  <name>Task 2: Integrate TieredSearcher into HybridSearcher</name>
  <files>
    crates/memd/src/store/hybrid.rs
  </files>
  <action>
Modify HybridSearcher in crates/memd/src/store/hybrid.rs:

Add to HybridConfig:
- enable_tiered: bool (default true)
- tiered_config: Option(TieredSearcherConfig)

Add to HybridSearcher:
- tiered: Option(TieredSearcher(Self)) -- Self implements WarmTierSearch
- use crate::tiered::{TieredSearcher, SemanticCache, HotTier, AccessTracker}

Implement WarmTierSearch for HybridSearcher (or a wrapper):
- search(query_embedding, k) -> forwards to existing dense + sparse fusion
- get_embedding(chunk_id) -> retrieve from dense searcher if cached
- get_version() -> combine dense + sparse versions

Update HybridSearcher::new():
- If enable_tiered:
  - Create AccessTracker with default config
  - Create HotTier with access_tracker reference
  - Create SemanticCache with default config
  - Create TieredSearcher wrapping self as warm tier
  - Store in self.tiered

Update search_with_timing():
- If tiered is Some:
  - Embed query first
  - Call tiered.search(query_embedding, tenant_id, project_id, k)
  - Return TieredSearchResult converted to HybridSearchResult
- Else:
  - Use existing dense + sparse search path

Add method search_tiered_with_debug() -> (Vec(HybridSearchResult), TieredTiming, Vec(TierDecision)):
- For debug output with tier decisions

Add method run_tiered_maintenance():
- Calls tiered.run_maintenance() if tiered is Some

Note: This requires careful refactoring since HybridSearcher needs to be both the coordinator AND the warm tier. Consider creating WarmTierAdapter that wraps DenseSearcher + Bm25Index.
  </action>
  <verify>
cargo check passes
cargo test --lib store::hybrid -- existing tests still pass
  </verify>
  <done>
HybridSearcher integrates TieredSearcher for cache/hot/warm fallback.
WarmTierSearch trait implemented for warm tier access.
Existing hybrid search still works when tiered disabled.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tiered search into PersistentStore</name>
  <files>
    crates/memd/src/store/persistent.rs
  </files>
  <action>
Update PersistentStore in crates/memd/src/store/persistent.rs:

Add to PersistentStoreConfig:
- enable_tiered_search: bool (default true)

Update search_with_scores():
- Get tiered timing from hybrid_searcher if available
- Record tiered metrics via metrics.record_tiered_query()
- Return results with timing

Add method search_with_tier_info():
- Returns (Vec((MemoryChunk, f32)), TieredTiming, Option(Vec(TierDecision)))
- For MCP handlers that want debug info

Add method get_tiered_stats() -> Option(TieredStats):
- Returns hot tier stats, cache stats, access tracker stats
- Used by memory.metrics handler

Struct TieredStats:
- cache: CacheStats
- hot_tier: HotTierStats
- access_tracker_entries: usize
- tiered_metrics: TieredMetrics

Add method invalidate_chunk(chunk_id):
- Called on delete
- Propagates to hot_tier.demote() if present
- Propagates to cache.invalidate_chunks([chunk_id])
- Updates version

Update delete() to call invalidate_chunk().

Add method run_maintenance():
- Calls hybrid_searcher.run_tiered_maintenance() periodically
- Consider spawning a background task

Add unit tests for tiered integration.
  </action>
  <verify>
cargo check passes
cargo test --lib store::persistent -- existing tests still pass
New tiered functionality covered by tests
  </verify>
  <done>
PersistentStore exposes tiered search with metrics.
Chunk deletion propagates to cache/hot tier invalidation.
TieredStats available for MCP handlers.
Maintenance method ready for periodic calls.
  </done>
</task>

</tasks>

<verification>
- cargo check passes
- cargo test --lib store:: passes all tests
- HybridSearcher uses TieredSearcher when enabled
- Cache hits skip warm tier search
- Hot tier hits reduce warm tier load
- TieredMetrics track cache/hot/warm performance
- Version propagation works for invalidation
- Existing behavior preserved when tiered disabled
</verification>

<success_criteria>
Tiered integration complete:
1. HybridSearcher uses cache/hot/warm fallback
2. TieredMetrics track tier-specific performance
3. PersistentStore exposes tiered stats
4. Chunk deletion invalidates cache and hot tier
5. WarmTierSearch trait bridges to existing search
6. All existing tests still pass
7. New tiered functionality tested
</success_criteria>

<output>
After completion, create `.planning/phases/05-hot-tier-+-cache---hot-cache,-semantic-cache,-promotion/05-04-SUMMARY.md`
</output>
