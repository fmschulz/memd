# Local cargo config.
# Keep the toolchain behavior predictable across machines.

[target.x86_64-unknown-linux-gnu]
linker = "cc"
[build]
jobs = 64

[registries.crates-io]
protocol = "sparse"

[net]
git-fetch-with-cli = true
retry = 3
