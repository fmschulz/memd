# alpha_gateway

Pristine benchmark fixture for the `bench_v2_alpha` tenant. Nothing else
lives here on purpose — if a benchmark question can be answered by
grep-ing this directory, that's a benchmark failure, not a memd success.
