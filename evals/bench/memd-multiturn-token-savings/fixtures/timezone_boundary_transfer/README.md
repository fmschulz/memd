# Scheduler Fixture

This fixture contains a small scheduler helper and focused regression tests.
The current implementation has a boundary-time bug. Fix the implementation
without changing the public function names.

Run:

```bash
python3 -m unittest -q
```
